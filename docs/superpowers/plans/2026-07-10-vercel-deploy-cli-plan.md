# Plano — Deploy da Vercel via CLI

1. Trocar o step de deploy no workflow por `npx vercel deploy`.
2. Atualizar `docs/GIT-WORKFLOW.md` com o novo fluxo.
3. Gerar relatório HTML.
4. Atualizar `HANDOFF.md`.
5. Validar com PR e merge em `main`.
