# Plano — Deploy da Vercel via CLI

1. Criar `tools/vercel-proxy` com binário `vercel` 25.1.0.
2. Registrar o proxy como devDependency no `package.json`.
3. Regenerar `package-lock.json`.
4. Gerar relatório HTML.
5. Atualizar `HANDOFF.md`.
6. Validar com o comando do workflow e abrir PR.
