# Veredito — P0.5 hardening do workflow

## Síntese do Chairman

**Consenso:** corrigir só os pontos que impedem confiança no P0: docs strict real no `check`, sessão P0 não continuar automaticamente, auto-merge sem bypass admin, e cards/docs alinhados.

**Veredito Final:** Faça.

**Próximos Passos:** aplicar menor diff em scripts/workflows/docs, validar com `verify-docs:strict`, testes unitários dos scripts e `npm run check`.
